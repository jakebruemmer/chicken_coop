<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New egg</h1>

  <%= render "form", egg: @egg, url: "/chickens/#{params[:chicken_id]}/eggs", method: :post %>
  <%= link_to "Back to this chicken", chicken_path(@chicken) %>
</div>
