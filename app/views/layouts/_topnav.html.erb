<nav>
  <div class="container flex flex-wrap items-center justify-between mx-auto w-full flex">
    <ul class="flex mx-auto">
      <li class="mr-6"><%= link_to "Home", root_path %></li>
      <% if user_signed_in? %>
        <li class="mr-6"><%= link_to "Account Home", user_path(current_user) %></li>
        <li class="mr-6"><%= link_to "Your Coops", coops_path %></li>
        <li class="mr-6"><%= link_to "Your Chickens", chickens_path %></li>
        <li class="mr-6"><%= link_to "Sign out", destroy_user_session_path, data: { turbo_method: :delete } %></li>
      <% end %>
      <% if !user_signed_in? %>
        <li class="mr-6"><%= link_to "Sign in", new_user_session_path, method: :get %></li>
      <% end %>
    </ul>
</div>
</nav>