<!DOCTYPE html>
<html>
  <head>
    <title>ChickenCoop</title>
    <script src="https://js.stripe.com/v3/"></script>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="container mx-auto">
      <nav><%= render "layouts/topnav" %></nav>
      <% if notice %>
        <p class="bg-green-50 text-green-500 px-3 py-2 font-medium rounded-lg mt-3"><%= notice %></p> 
      <% end %>
      <% if alert %>
        <p class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-lg mt-3"><%= alert %></p>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
